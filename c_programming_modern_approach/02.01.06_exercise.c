/**
 * Why is it not a good idea for
 * an identifier to contain more than
 * one adjacent underscore (as in current__balance, for example)?
 */

 /* ANSWER: it is confusing with the same identifier with one underscore */