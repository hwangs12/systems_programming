/**
 * Only one of the expressions ++i and i++ is exactly the same as
 * (i += 1): which is it? Justify your answer
 * value of i in i++ expression does not change
 * but i+=1 value of i is 1 incremented to it. 
 * Hence, it is the same as ++i;
 *
 */

#include <stdio.h>

int main(void)
{
    
}