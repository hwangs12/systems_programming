/**
 * Supply parentheses to show how C cmopiler
 * would interpret each of the 
 * following expressions
 * 
 * a) a* b - c * d + e -> (((a*b) - (c*d)) + e)
 * b) a/b%c/d -> (((a/b)%c)/d)
 * c) -a-b+c-+d -> ((((-a)-b)+c)-(+d))
 * d) a * -b/c -> ((a*(-b))/c)
 */

 /**
  * NEEDS TO BE TESTED
  */