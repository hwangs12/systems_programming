/**
 * Write a single expression
 * whose value is either -1, 0, or +1
 * depending on whether i is less than
 * equal to or greater than j
 */

#include <stdio.h>

int main(void)
{
    int i, j;
    (i > j) - (i < j);
    // how did someone find
    // this solution





    return 0;
}